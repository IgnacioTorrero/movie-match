name: Build & Test Backends

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [auth-service, movie-service, rating-service, recommendation-service]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          cd movie-match-api/${{ matrix.service }}
          npm ci

      - name: Run tests
        run: |
          cd movie-match-api/${{ matrix.service }}
          npm run test

      - name: Build Docker image
        run: |
          docker build -t ${{ matrix.service }} -f movie-match-api/${{ matrix.service }}/Dockerfile .
